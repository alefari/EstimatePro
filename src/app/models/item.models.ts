export interface Item {
    id: string,
    nombre?: string,
    categoria?: string,
    subcategoria?: string,
    unidad?: string,
    laborRate?: number,
    materialRate?: number,
    equipmentRate?: number,
    fecha: string,
    idUsuario?: string,
}
